# AutoPost Studio 操作文档

## 1. 环境要求

- macOS、Windows 或 Linux。
- 已安装本机 Google Chrome。
- Node.js 20 或更高版本。

## 2. 安装与启动

```bash
npm install
npm start
```

项目是离线 Electron 应用。发布时需要平台网页和本机 Chrome 可以访问网络。

## 3. 首次设置

进入“设置中心”：

- 选择缓存根目录。每个平台、每个账号会自动使用独立的 Chrome Profile。
- 设置浏览器窗口宽度和高度，新启动的账号浏览器会使用该尺寸。
- 设置同时上传账号数。数值越大，CPU 和内存占用越高，普通设备建议设置为 1 到 2。

## 4. 账号管理

在“账号列表”中添加平台账号，填写账号名称、备注和可选代理。点击“登录”后，软件会在独立 Chrome 窗口中打开平台页面。

账号登录状态保存在对应缓存目录中。编辑账号时平台不可修改，账号名称必须在同一平台唯一；修改名称会同步重命名缓存目录。删除账号会关闭对应 Chrome 并删除完整缓存目录，请谨慎操作。

## 5. 创建和编辑发布任务

在内容库或发布队列中创建任务，选择视频或文章，填写标题、正文、素材、封面、话题、标签、好友、合集和发布时间，并选择目标账号。

发布队列支持二次编辑。编辑后任务会回到待发布状态，旧的发布结果会被清除。

## 6. 发布流程

点击“填充并等待确认”后，软件会：

1. 按设置的并发数启动账号浏览器。
2. 直接进入平台发布页面并检测登录状态。
3. 填充素材、标题、正文、封面和发布设置。
4. 所有账号素材准备完成后显示统一确认弹窗。
5. 在弹窗中逐账号选择重试或删除，最后点击“确认发布”。
6. 只有确认保留的账号后，RPA 才会点击平台发布按钮并等待结果。

账号列表和发布队列右侧日志会记录 RPA 操作过程、缓存目录、调试端口和失败原因。

## 7. 安全说明

- 不要把账号缓存、Cookies、代理密码、视频素材或本地状态文件提交到 Git。
- 不要在公开仓库中保存 `.env`、访问令牌或平台登录信息。
- 平台可能因网络、设备指纹、账号风控或页面改版阻止访问；遇到安全拦截时应在平台官方页面人工确认账号状态。
- 关闭软件时会自动关闭由本软件打开的账号 Chrome 会话。

## 8. 开发检查

```bash
npm run build
npm audit --omit=dev
```

主进程和 RPA 模块可以使用 `node --check` 进行语法检查。
